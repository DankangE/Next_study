'use client';

import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ClientPostHeader: React.FC = () => (
  <>
    <Title>Post</Title>
  </>
);

export default ClientPostHeader;